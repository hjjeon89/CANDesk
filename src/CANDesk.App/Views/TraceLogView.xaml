<UserControl x:Class="CANDesk.App.Views.TraceLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="8,6">
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="150" /></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Text="FILTER" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="0,0,8,0" />
      <Border Style="{StaticResource SearchBoxContainerStyle}" Width="150">
        <TextBox Style="{StaticResource SearchTextBoxStyle}" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" ToolTip="CAN ID, e.g. 0x100" />
      </Border>
      <CheckBox Content="Show RX" IsChecked="{Binding ShowRx}" VerticalAlignment="Center" Margin="14,0,0,0" />
      <CheckBox Content="Show TX" IsChecked="{Binding ShowTx}" VerticalAlignment="Center" Margin="12,0,0,0" />
      <CheckBox Content="Errors only" IsChecked="{Binding ErrorsOnly}" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource FailBrush}" />
    </StackPanel>
    <Border Grid.Row="1" Style="{StaticResource PanelCardStyle}">
      <DataGrid ItemsSource="{Binding FramesView}" SelectedItem="{Binding SelectedFrame}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" BorderThickness="0" EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
        <DataGrid.Columns>
          <DataGridTextColumn Header="No" Binding="{Binding Index}" Width="50" />
          <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=HH:mm:ss.fff}" Width="100" />
          <DataGridTemplateColumn Header="CAN ID" Width="80">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Border Style="{StaticResource IdBadgeStyle}" HorizontalAlignment="Left">
                  <TextBlock Style="{StaticResource IdBadgeTextStyle}" Text="{Binding Id}" />
                </Border>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTemplateColumn Header="Dir" Width="55">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Border Style="{StaticResource DirBadgeBorderStyle}">
                  <TextBlock Style="{StaticResource DirBadgeTextStyle}" Text="{Binding Direction}" />
                </Border>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="45" />
          <DataGridTextColumn Header="Payload (Hex)" Binding="{Binding Data}" Width="220">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{DynamicResource MonoFontFamily}" />
                <Setter Property="VerticalAlignment" Value="Center" />
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTextColumn Header="Decoded Summary / Signals" Binding="{Binding Summary}" Width="*" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>
    <Border Grid.Row="2" Margin="0,8,0,0" Style="{StaticResource PanelCardStyle}" Padding="10">
      <StackPanel>
        <DockPanel>
          <TextBlock Text="Selected Frame Byte Inspector" Foreground="{DynamicResource TextSubBrush}" FontWeight="SemiBold" FontSize="11" />
          <Border DockPanel.Dock="Right" Style="{StaticResource IdBadgeStyle}" HorizontalAlignment="Right">
            <TextBlock Style="{StaticResource IdBadgeTextStyle}" Text="{Binding SelectedFrame.Id}" />
          </Border>
        </DockPanel>
        <ItemsControl ItemsSource="{Binding SelectedFrame.Bytes}" Margin="0,8,0,0" AlternationCount="64">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,5,0" Padding="7,4" MinWidth="32">
                <StackPanel HorizontalAlignment="Center">
                  <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=(ItemsControl.AlternationIndex), StringFormat='B{0}'}" FontFamily="{DynamicResource MonoFontFamily}" FontSize="9" Foreground="{DynamicResource MutedTextBrush}" HorizontalAlignment="Center" />
                  <TextBlock Text="{Binding}" FontFamily="{DynamicResource MonoFontFamily}" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="{Binding SelectedFrame.Summary}" Margin="0,8,0,0" Foreground="{DynamicResource PassBrush}" FontFamily="{DynamicResource MonoFontFamily}" TextWrapping="Wrap" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
