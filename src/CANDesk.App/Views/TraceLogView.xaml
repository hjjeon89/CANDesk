<UserControl x:Class="CANDesk.App.Views.TraceLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="8,6">
    <Grid.RowDefinitions><RowDefinition Height="32" /><RowDefinition Height="*" /><RowDefinition Height="140" /></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
      <TextBlock Text="FILTER" Style="{StaticResource CaptionTextStyle}" Margin="0,0,8,0" />
      <TextBox Width="145" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" ToolTip="CAN ID, e.g. 0x100" />
      <CheckBox Content="Show RX" IsChecked="{Binding ShowRx}" Margin="12,0,0,0" />
      <CheckBox Content="Show TX" IsChecked="{Binding ShowTx}" Margin="10,0,0,0" />
      <CheckBox Content="Errors only" IsChecked="{Binding ErrorsOnly}" Margin="10,0,0,0" Foreground="{DynamicResource FailBrush}" />
    </StackPanel>
    <DataGrid Grid.Row="1" ItemsSource="{Binding FramesView}" SelectedItem="{Binding SelectedFrame}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="No" Binding="{Binding Index}" Width="50" />
        <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=HH:mm:ss.fff}" Width="100" />
        <DataGridTextColumn Header="CAN ID" Binding="{Binding Id}" Width="80" />
        <DataGridTextColumn Header="Dir" Binding="{Binding Direction}" Width="55" />
        <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="45" />
        <DataGridTextColumn Header="Payload (Hex)" Binding="{Binding Data}" Width="220" />
        <DataGridTextColumn Header="Decoded Summary / Signals" Binding="{Binding Summary}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>
    <Border Grid.Row="2" Margin="0,8,0,0" Padding="10" Background="{DynamicResource PanelSubBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
      <StackPanel>
        <TextBlock Text="Selected Frame Byte Inspector" Foreground="{DynamicResource TextSubBrush}" FontWeight="SemiBold" />
        <ItemsControl ItemsSource="{Binding SelectedFrame.Bytes}" Margin="0,7,0,0"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,5,0" Padding="7,4"><TextBlock Text="{Binding}" FontFamily="Consolas" Foreground="{DynamicResource AccentBrush}" /></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
        <TextBlock Text="{Binding SelectedFrame.Summary}" Margin="0,7,0,0" Foreground="{DynamicResource PassBrush}" FontFamily="Consolas" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
