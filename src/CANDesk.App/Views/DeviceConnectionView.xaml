<UserControl x:Class="CANDesk.App.Views.DeviceConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border Background="{DynamicResource SurfaceBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="0,0,0,1"
          Padding="12,7">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <DockPanel LastChildFill="False">
        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
          <TextBlock Text="VENDOR" Style="{StaticResource CaptionTextStyle}" Margin="0,0,6,0" />
          <ComboBox Width="105" ItemsSource="{Binding Vendors}" SelectedItem="{Binding SelectedVendor}" />
          <TextBlock Text="CHANNEL" Style="{StaticResource CaptionTextStyle}" Margin="12,0,6,0" />
          <ComboBox Width="150" ItemsSource="{Binding Channels}" SelectedItem="{Binding SelectedChannel}" />
          <Border Style="{StaticResource ToolbarSeparatorStyle}" />
          <ToggleButton Content="Classic CAN" IsChecked="{Binding IsClassicMode, Mode=TwoWay}" />
          <ToggleButton Content="CAN FD" IsChecked="{Binding IsFdMode, Mode=TwoWay}" Margin="3,0,0,0" />
          <Button Content="Connect"
                  Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                  Style="{StaticResource PrimaryButtonStyle}"
                  Margin="10,0,0,0" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button Content="Start / Pause"
                  Command="{Binding DataContext.ToggleCaptureCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
          <Button Content="Clear"
                  Command="{Binding DataContext.ClearTraceCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                  Margin="5,0,0,0" />
          <ToggleButton Content="Auto Scroll"
                        IsChecked="{Binding DataContext.IsAutoScroll, RelativeSource={RelativeSource AncestorType=Window}}"
                        Margin="5,0,0,0" />
        </StackPanel>
      </DockPanel>
      <Border Grid.Row="1"
              Margin="0,8,0,0"
              Padding="8,6"
              Background="{DynamicResource PanelBrush}"
              BorderBrush="{DynamicResource BorderSubBrush}"
              BorderThickness="1"
              CornerRadius="6">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="330" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="330" />
          </Grid.ColumnDefinitions>
          <TextBlock Text="NOMINAL / ARBITRATION" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="0,0,10,0" />
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <ComboBox Width="170" ItemsSource="{Binding NominalPresets}" SelectedItem="{Binding SelectedNominalTiming}" />
            <ToggleButton Content="Custom" IsChecked="{Binding UseCustomNominalTiming}" Margin="6,0,0,0" />
            <TextBox Width="64" Margin="6,0,0,0" Text="{Binding CustomNominalBitrateKbps, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding UseCustomNominalTiming}" ToolTip="Nominal bitrate (kbps)" />
            <TextBox Width="52" Margin="4,0,0,0" Text="{Binding CustomNominalSamplePoint, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding UseCustomNominalTiming}" ToolTip="Nominal sample point (%)" />
          </StackPanel>
          <Border Grid.Column="2" Style="{StaticResource ToolbarSeparatorStyle}" />
          <TextBlock Grid.Column="3" Text="DATA PHASE (CAN FD)" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding IsFdMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
          <StackPanel Grid.Column="4" Orientation="Horizontal" Visibility="{Binding IsFdMode, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ComboBox Width="170" ItemsSource="{Binding DataPresets}" SelectedItem="{Binding SelectedDataTiming}" />
            <ToggleButton Content="Custom" IsChecked="{Binding UseCustomDataTiming}" Margin="6,0,0,0" />
            <TextBox Width="64" Margin="6,0,0,0" Text="{Binding CustomDataBitrateKbps, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding UseCustomDataTiming}" ToolTip="Data bitrate (kbps)" />
            <TextBox Width="52" Margin="4,0,0,0" Text="{Binding CustomDataSamplePoint, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding UseCustomDataTiming}" ToolTip="Data sample point (%)" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
  </Border>
</UserControl>
