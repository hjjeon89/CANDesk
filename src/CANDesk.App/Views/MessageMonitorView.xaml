<UserControl x:Class="CANDesk.App.Views.MessageMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Border Padding="8,6"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource BorderSubBrush}"
            BorderThickness="1"
            CornerRadius="6">
      <DockPanel LastChildFill="False">
        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
          <TextBlock Text="MONITOR FILTER" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="0,0,8,0" />
          <Border Style="{StaticResource SearchBoxContainerStyle}" Width="145">
            <TextBox Style="{StaticResource SearchTextBoxStyle}" Text="{Binding IdFilter, UpdateSourceTrigger=PropertyChanged}" ToolTip="CAN ID, e.g. 0x100" />
          </Border>
          <CheckBox Content="Show RX" IsChecked="{Binding ShowRx}" VerticalAlignment="Center" Margin="12,0,0,0" />
          <CheckBox Content="Show TX" IsChecked="{Binding ShowTx}" VerticalAlignment="Center" Margin="10,0,0,0" />
          <CheckBox Content="Errors only" IsChecked="{Binding ErrorsOnly}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource FailBrush}" />
          <Button Content="⟲ Clear" Command="{Binding ClearCommand}" Margin="12,0,0,0" ToolTip="Remove all rows from the monitor" />
        </StackPanel>
        <TextBlock DockPanel.Dock="Right" Text="Latest value per CAN ID" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" />
      </DockPanel>
    </Border>
    <Border Grid.Row="1" Margin="0,8,0,0" Style="{StaticResource PanelCardStyle}">
      <DataGrid ItemsSource="{Binding RowsView}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                IsReadOnly="True"
                BorderThickness="0"
                EnableRowVirtualization="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                ScrollViewer.CanContentScroll="True">
        <DataGrid.Columns>
          <DataGridTemplateColumn Header="CAN ID" Width="90">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Border Style="{StaticResource IdBadgeStyle}" HorizontalAlignment="Left">
                  <TextBlock Style="{StaticResource IdBadgeTextStyle}" Text="{Binding Id}" />
                </Border>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTextColumn Header="Message" Binding="{Binding MessageName}" Width="150" />
          <DataGridTextColumn Header="Dir" Binding="{Binding Direction}" Width="55" />
          <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50" />
          <DataGridTextColumn Header="Payload (Hex)" Binding="{Binding Payload}" Width="220">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{DynamicResource MonoFontFamily}" />
                <Setter Property="VerticalAlignment" Value="Center" />
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTextColumn Header="Decoded Summary / Signals" Binding="{Binding Summary}" Width="*" />
          <DataGridTextColumn Header="Cycle (ms)" Binding="{Binding CycleTimeMs, StringFormat=F2}" Width="90" />
          <DataGridTextColumn Header="Count" Binding="{Binding ReceiveCount}" Width="90" />
          <DataGridTextColumn Header="Last Received" Binding="{Binding LastReceived, StringFormat=HH:mm:ss.fff}" Width="130" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</UserControl>
