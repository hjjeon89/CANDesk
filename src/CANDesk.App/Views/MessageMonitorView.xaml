<UserControl x:Class="CANDesk.App.Views.MessageMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Border Padding="8,6"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource BorderSubBrush}"
            BorderThickness="1"
            CornerRadius="6">
      <DockPanel LastChildFill="False">
        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
          <TextBlock Text="MONITOR FILTER" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" FontSize="10" FontWeight="SemiBold" Margin="0,0,8,0" />
          <TextBox Width="145" Text="{Binding IdFilter, UpdateSourceTrigger=PropertyChanged}" ToolTip="CAN ID, e.g. 0x100" />
          <CheckBox Content="Show RX" IsChecked="{Binding ShowRx}" Margin="12,0,0,0" />
          <CheckBox Content="Show TX" IsChecked="{Binding ShowTx}" Margin="10,0,0,0" />
          <CheckBox Content="Errors only" IsChecked="{Binding ErrorsOnly}" Margin="10,0,0,0" Foreground="{DynamicResource FailBrush}" />
        </StackPanel>
        <TextBlock DockPanel.Dock="Right" Text="Latest value per CAN ID" Foreground="{DynamicResource MutedTextBrush}" />
      </DockPanel>
    </Border>
    <Border Grid.Row="1" Margin="0,8,0,0" Style="{StaticResource PanelCardStyle}">
      <DataGrid ItemsSource="{Binding Rows}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                IsReadOnly="True"
                EnableRowVirtualization="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                ScrollViewer.CanContentScroll="True">
        <DataGrid.Columns>
          <DataGridTextColumn Header="CAN ID" Binding="{Binding Id}" Width="90" />
          <DataGridTextColumn Header="Message" Binding="{Binding MessageName}" Width="150" />
          <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50" />
          <DataGridTextColumn Header="Payload (Hex)" Binding="{Binding Payload}" Width="*" />
          <DataGridTextColumn Header="Cycle (ms)" Binding="{Binding CycleTimeMs, StringFormat=F2}" Width="90" />
          <DataGridTextColumn Header="Count" Binding="{Binding ReceiveCount}" Width="90" />
          <DataGridTextColumn Header="Last Received" Binding="{Binding LastReceived, StringFormat=HH:mm:ss.fff}" Width="130" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</UserControl>
